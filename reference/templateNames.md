# for backwards compatibility

for backwards compatibility

## Usage

``` r
templateNames(project = NULL)
```

## Arguments

- project:

  name of the project of requested mapping. If not specified, all
  existing mappings will be returned
